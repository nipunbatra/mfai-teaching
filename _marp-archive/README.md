# Marp archive (retired August 2026)

The course's original slide system — Marp Markdown decks (`slides/`), their matplotlib
figure generators (`diagrams/`), and the cream/rust-styled outputs (`figures/`) — retired
when the course pivoted to the Typst system shared with dl-teaching (see `common/` and
`lectureN/` at the repo root). Kept for reference; never build from here.
